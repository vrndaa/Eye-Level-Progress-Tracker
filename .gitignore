# Dependencies — npm can recreate this, so we don't track it
node_modules/

# Secrets — NEVER commit these (Google credentials come later)
*.key
*.pem
service-account*.json
*credentials*.json
.env
.env.*

# Real student PII — kept locally only, never committed/pushed
classroom-screenshots/

# Mac / editor junk
.DS_Store
